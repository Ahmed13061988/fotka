
<h2>Recent Uploads</h2>

<div class="card-columns">
  <% @photos.each do |image| %>
    <div class="card">
      <div class="card-body p-1">
        <%= image_tag photo.photo, class: 'd-block w-100' %>

        <p class="my-1"><%= photo.description %></p>

        <%= link_to "View Image", photo_path(image.id), class: 'btn btn-secondary stretched-link' %>
      </div>
    </div>
  <% end %>
</div>
